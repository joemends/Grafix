let appearanceData={};
const T$=id=>document.getElementById(id); const Tstatus=(m)=>{if(T$('appearanceStatus'))T$('appearanceStatus').textContent=m||''};
const themeDefaults=window.DEFAULT_SITE_APPEARANCE||{theme:{},features:{},content:{}};
async function loadAppearance(){const s=(await getSettings()).site||{};appearanceData=JSON.parse(JSON.stringify(s));const t={...themeDefaults.theme,...(s.theme||{})},c={...themeDefaults.content,...(s.content||{})},f={...themeDefaults.features,...(s.features||{})};
 Object.entries(t).forEach(([k,v])=>{const el=T$('th_'+k);if(el)el.value=v});Object.entries(c).forEach(([k,v])=>{const el=T$('ct_'+k);if(el)el.value=v});Object.entries(f).forEach(([k,v])=>{const el=T$('ft_'+k);if(el)el.checked=v!==false});}
function val(id,fallback=''){return T$(id)?.value ?? fallback}
T$('appearanceForm')?.addEventListener('submit',async e=>{e.preventDefault();Tstatus('Saving...');try{const old=(await getSettings()).site||{};const theme={...(old.theme||{})}, content={...(old.content||{})}, features={...(old.features||{})};['bg','surface','surface2','text','muted','line','accent','accentSoft','hero','about','projects','artboard','contact','footer','font','headingFont','radius'].forEach(k=>{const el=T$('th_'+k);if(el)theme[k]=el.value});Object.keys(themeDefaults.content).forEach(k=>{const el=T$('ct_'+k);if(el)content[k]=el.value});Object.keys(themeDefaults.features).forEach(k=>{const el=T$('ft_'+k);if(el)features[k]=el.checked});const site={...old,theme,content,features};const {error}=await supabaseClient.from('site_settings').upsert({key:'site',value:site,updated_at:new Date().toISOString()});if(error)throw error;appearanceData=site;Tstatus('Saved. Refresh the public website to see the changes.')}catch(err){Tstatus(err.message)}});
loadAppearance();
